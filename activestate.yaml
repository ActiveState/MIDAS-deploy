project: https://platform.activestate.com/shaunl/MIDAS-Perl?branch=main&commitID=a6728b1b-c544-433e-8d86-2124b9b8000c

scripts:
  - name: setup
    description: Runs the perl script to set up file extension associations and start menu shortcut pointing to the perl executable
    language: perl
    if: eq .OS.Name "Windows"
    value: |
      use lib q(.);
      use Setup qw(create_internet_shortcuts create_shortcuts create_file_assoc set_system_user_env);
      create_internet_shortcuts();
      create_shortcuts();
      create_file_assoc();
      set_system_user_env();
      print "Successfully created Desktop Shortcuts and File Associations.\n\n"
events:
 - name: first-activate
   if: eq .OS.Name "Windows"
   value: perl $scripts.setup.path()
